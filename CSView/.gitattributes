# The demo and bzip2 fixtures are byte-exact inputs the tests parse; line
# ending translation would corrupt them.
tests/data/** binary

# Batch files need CRLF to be reliable on Windows.
*.bat text eol=crlf
